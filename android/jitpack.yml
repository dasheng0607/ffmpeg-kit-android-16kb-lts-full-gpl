jdk:
  - openjdk17
before_install:
  - chmod +x gradlew
install:
  # This forces JitPack to run the build from the ROOT context
  - ./gradlew :ffmpeg-kit-android-lib:publishToMavenLocal -Pgroup=com.github.VineshChauhan24 -Pversion=1.0.1