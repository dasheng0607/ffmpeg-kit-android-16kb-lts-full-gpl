jdk:
  - openjdk17
before_install:
  - chmod +x gradlew
install:
  # This ensures JitPack runs the publishing task from your root
  - ./gradlew :ffmpeg-kit-android-lib:publishToMavenLocal -Pgroup=com.github.VineshChauhan24 -Pversion=v1.0.1