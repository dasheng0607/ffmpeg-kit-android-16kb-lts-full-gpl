jdk:
  - openjdk17
before_install:
  - chmod +x gradlew
install:
  # This command forces JitPack to stay in the root and run the specific library task
  - ./gradlew :ffmpeg-kit-android-lib:publishToMavenLocal -Pgroup=com.github.VineshChauhan24 -Pversion=1.0.5